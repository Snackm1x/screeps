var TFighter = {
    run: function(room){
        // var towers = room.find(FIND_STRUCTURES, {
        //             filter: (structure) => {
        //                 return (structure.structureType == STRUCTURE_TOWER);
        //             }
        //     });
            
        // if(towers){
        //     var t = towers[0];
        //     var targets = room.find(FIND_HOSTILE_CREEPS);
        //     var target = t.pos.findClosestByRange(targets);
        //     t.attack(target);
        // }
    }
};

module.exports = TFighter;